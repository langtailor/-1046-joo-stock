# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

# 정본은 표지(index.markdown) 제목이다. about.markdown 「사업명」과 CLAUDE.md
# 사업명은 표지 제목 전문과 일치시킨다.
# 단 이 title만 예외로 성과 지표 괄호 "(Latency -40%, Recall@5 92% 달성)"를 뺀다.
# 이 값은 매 페이지 헤더와 <title> 태그에 들어가는데, 전문을 쓰면 헤더가 두 줄로
# 감기고 탭 제목이 잘린다. 의도된 차이이므로 표지 제목에 맞춰 되돌리지 말 것.
title: 금융 공시(DART/SEC) 복합 문서 구조화 및 Hybrid RAG pipeline 구축
email: taylor@ragtailor.com
description: >- # this means to ignore newlines until "baseurl:"
  금융 공시(DART/SEC) 복합 문서 구조화 및 Hybrid RAG pipeline 구축 개발 제안서.
  개발 기간 2026년 8월 20일 ~ 10월 27일.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://fin.ragtaylor.com" # the base hostname & protocol for your site, e.g. http://example.com
github_username:  ragtailor

# Build settings
theme: minima

# 개발 로그 URL. 카테고리(한국어)가 경로에 들어가면 퍼센트 인코딩되므로 고정 경로를 사용한다.
permalink: /log/:year/:month/:day/:title/

# 데모 단계 검색 색인 차단. 프로덕션(fin.ragtailor.com) 전환 시 false로 변경한다.
# _includes/head.html에서 robots 메타를 출력한다.
noindex: true

# 헤더 메뉴에 노출할 페이지와 순서. 지정하지 않으면 minima가 site.pages를
# 순서 없이 전부 나열한다. 각 장은 목차와 장 하단 이전/다음 링크로 이동한다.
header_pages:
  - toc.markdown
  - log.markdown
  - todo.markdown
plugins:
  - jekyll-feed

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
exclude:
  - CLAUDE.md

# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
