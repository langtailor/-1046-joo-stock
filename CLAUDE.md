# CLAUDE.md

국가 금융데이터 미디어 자동화 및 차세대 대민 서비스 플랫폼 구축 — 제안서/문서 사이트.

## 프로젝트 기본 정보

| 항목 | 값 |
| --- | --- |
| 사업명 | 국가 금융데이터 미디어 자동화 및 차세대 대민 서비스 플랫폼 구축 |
| 개발 기간 | 2026-08-20 (목) ~ 2026-10-27 (화) · 69일 / 약 10주 |
| 개발팀 | 테일러, 왓슨, 앨리스, 토마스 (4명) |
| 산출물 | Jekyll 정적 사이트 (GitHub Pages 배포) |
| 리포 | `github.com/ragtailor/fin.ragtaylor.com` |

---

## ⚠️ 도메인 규칙 — 가장 먼저 확인할 것

**철자가 한 글자(`i`)만 다른 두 도메인이 공존합니다. 절대 혼용하지 마십시오.**

| 도메인 | 철자 | 용도 |
| --- | --- | --- |
| `ragtailor.com` | t-a-**i**-l-o-r (재단사) | **실제 조직 / 프로덕션 계열** |
| `ragtaylor.com` | t-a-y-l-o-r (사람 이름) | **데모 / 스테이징 전용** |

### 각각의 쓰임

**`ragtailor.com` — 실제(real) 계열**
- GitHub 계정: `ragtailor`
- 연락처 이메일: `taylor@ragtailor.com`
- **최종 프로덕션 도메인: `fin.ragtailor.com`** ← 사업 완료 후 전환 대상

**`ragtaylor.com` — 데모(demo) 계열**
- 리포 이름: `fin.ragtaylor.com`
- **현재 배포 도메인: `fin.ragtaylor.com`**
- `_config.yml`의 `url` 현재 값: `https://fin.ragtaylor.com`

### 판단 기준

- 리포 이름이 `fin.ragtaylor.com`인 것은 **의도된 것**입니다. 오타가 아니므로 고치지 마십시오.
- `email`·`github_username`이 `ragtailor`인 것도 **의도된 것**입니다. 실제 조직 계열이기 때문입니다.
- 즉 **한 리포 안에 두 철자가 동시에 존재하는 것이 정상 상태**입니다. 불일치로 판단해 한쪽으로 통일하지 마십시오.
- 새 값을 넣을 때: 사람·조직·계정에 관한 것이면 `ragtailor`, 현재 배포 주소에 관한 것이면 `ragtaylor`.

---

## 배포 전략

### 1단계 — 데모 (현재 ~ 사업 완료 전)

- 배포 대상: `fin.ragtaylor.com`
- `_config.yml`의 `url`은 `https://fin.ragtaylor.com`으로 **유지**
- 미완성 문서가 프로덕션 도메인에 노출되지 않게 하는 것이 목적
- 검색 색인 차단(`noindex`)을 적용한다 — 아래 미결 항목 참조

**이 단계에서 프로덕션 도메인으로 전환하지 마십시오.** 목적·기대효과 등 본문이 비어 있는 상태의 국가사업 문서가 실제 사업 도메인으로 색인되면 되돌리기 어렵습니다.

### 2단계 — 프로덕션 전환 (사업 완료 시점)

전환은 다음 순서로만 진행합니다. 사용자의 명시적 지시 없이 착수하지 마십시오.

1. `fin.ragtailor.com` DNS 레코드 생성 — **1단계 중에 미리 만들어 두면** 전파 대기 없이 컷오버 가능
2. 리포 루트에 `CNAME` 파일 생성 → 내용 `fin.ragtailor.com`
3. `_config.yml`의 `url` → `https://fin.ragtailor.com`
4. `_config.yml`의 `noindex: true` → `false` (메타 출력은 `_includes/head.html`이 담당)
5. GitHub Pages 설정에서 custom domain 지정 + **Enforce HTTPS** 활성화
6. 리포 이름 변경은 **불필요** (GitHub Pages는 리포 이름과 도메인이 달라도 동작)

전환 후에도 데모 도메인은 스테이징으로 유지합니다.

---

## Jekyll 문서 작성 규칙

### 문서 구조

사이트는 제안서 한 부입니다. 목차 5장 구성을 기준으로 하며, 임의로 장을 추가·삭제하지 않습니다.

| 장 | 페이지 | permalink |
| --- | --- | --- |
| 표지 | `index.markdown` | `/` |
| 목차 | `toc.markdown` | `/toc/` |
| 1. 사업 개요 | `about.markdown` | `/about/` |
| 2. 개발 요구 사항 | *미생성* | `/requirements/` |
| 3. 주요 개발 수행 지침 | *미생성* | `/guidelines/` |
| 4. 개발 일정 및 추진 체계 | *미생성* | `/schedule/` |
| 5. 부록 | *미생성* | `/appendix/` |
| (부속) 개발 로그 목록 | `log.markdown` | `/log/` |

새 장을 만들 때는 `toc.markdown`의 항목과 **반드시 함께** 갱신합니다.

### front matter 규칙

```yaml
---
layout: page          # 표지만 layout: default
title: 2. 개발 요구 사항   # 목차의 장 제목과 정확히 일치시킬 것
permalink: /requirements/
---
```

### 내용 작성 규칙 — 중요

- **확정되지 않은 내용을 지어내지 마십시오.** 국가사업 문서이므로 추정으로 채운 문장이 그대로 제출될 위험이 있습니다.
- 미확정 항목은 반드시 아래 형식으로 비워 둡니다.

  ```markdown
  ## 사업 목적

  <!-- TODO: 확정 내용으로 교체 -->
  *작성 예정 — 제안서 1장 1) 사업 목적에 해당합니다.*
  ```

- **발주처·기관명을 임의로 표기하지 마십시오.** 목차 양식은 한국소프트웨어진흥원의 제안요청서 양식안을 참고한 것이며, 해당 기관은 **양식의 출처일 뿐 이 사업의 발주처가 아닙니다.** 실제 발주처는 사용자 확인 후에만 기입합니다.
- **날짜·기간·인원 등 수치는 계산해서 검증한 뒤** 기입합니다 (예: 69일은 `date`로 확인한 값).
- 본문은 한국어, 문체는 문어체(`~합니다` / `~하십시오`)로 통일합니다.

### 스타일

- 테마는 `minima` **2.5.2**. minima 2.x에는 `custom-head` 확장 지점이 **없습니다**(3.x 기능). `<head>`를 손대려면 `_includes/head.html`을 프로젝트에 두어 오버라이드해야 하며, 이미 그렇게 되어 있습니다. 그 외에는 페이지별 `<style>` 블록으로 확장합니다.
- 테마 gem 파일 자체를 수정하지 마십시오.
- 한글 줄바꿈이 어색해지지 않도록 제목·목차 항목에는 `word-break: keep-all`을 적용합니다.

---

## 진행사항 기록 규칙

### 개발 로그 — `_posts/`

주요 진척이 있을 때마다 기록합니다. 파일명은 `YYYY-MM-DD-slug.markdown` 형식이며, **slug는 영문 소문자·하이픈**으로 씁니다 (한글 파일명은 URL이 퍼센트 인코딩되어 읽기 어려워집니다). 제목은 front matter의 `title`에 한국어로 씁니다.

```yaml
---
layout: post
title: "주차별 제목"
date: 2026-08-20 18:00:00 +0900
categories: 진행사항
---
```

글 URL은 `_config.yml`의 `permalink: /log/:year/:month/:day/:title/`로 고정되어 있습니다. **이 설정을 지우지 마십시오** — 기본값이면 한국어 카테고리가 URL 경로에 들어가 퍼센트 인코딩됩니다. 목록은 `/log/` 페이지가 `site.posts`를 순회해 자동 생성하므로, 글을 추가할 때 목록을 손볼 필요는 없습니다.

본문은 다음 세 항목을 고정 순서로 씁니다.

```markdown
## 완료
- 항목

## 진행 중
- 항목

## 다음 작업
- 항목
```

작성 시점 기준의 **사실만** 씁니다. 검증하지 않은 완료 처리를 하지 마십시오 — 서버가 떴다면 실제 HTTP 응답을 확인한 뒤 기록합니다.

### 미결 항목 — `todo.markdown` (`/todo/`)

프로젝트에 필요한 것 중 아직 해결되지 않은 항목을 한곳에 모읍니다. 사용자 확인이 필요한 질문도 여기 둡니다.

| 구분 | 항목 | 상태 | 비고 |
| --- | --- | --- | --- |
| 내용 | 사업 목적 확정 | 대기 | 1장 1) |

구분은 `내용` / `배포` / `환경` / `확인필요` 중 하나를 씁니다.

---

## 로컬 개발 환경

| 항목 | 값 |
| --- | --- |
| 호스트 | `DESKTOP-B09D4AN` (Windows 10 22H2 + WSL2 Ubuntu 26.04) |
| Ruby | 3.3.12 (rbenv, `rbenv global` 적용) |
| Jekyll / Bundler | 4.4.1 / 4.0.19 |

```bash
bundle exec jekyll serve --host 0.0.0.0 --port 4000
```

### 알아둘 것

- **`_config.yml`을 수정하면 `jekyll serve`가 자동 반영하지 않습니다.** 반드시 서버를 재시작하십시오. 다른 파일은 auto-regeneration으로 반영됩니다.
- **`sudo`는 비밀번호를 요구하며, TTY가 없는 환경에서는 실행되지 않습니다.** 패키지 설치 등이 필요하면 사용자에게 **Windows에서 Ubuntu(WSL) 터미널을 직접 열어** 실행하도록 요청하십시오. TTY 없이 시도하면 apt가 시작조차 되지 않고 로그에 흔적도 남지 않습니다.
- 설치·변경 결과는 **로그나 실제 파일로 검증**한 뒤 다음 단계로 넘어갑니다 (`dpkg -s`, 헤더 파일 존재, `/var/log/apt/history.log`, HTTP 응답 코드 등).
- **`Odyssey`는 이 프로젝트와 무관한 별개의 홈서버이며 현재 홀드 상태입니다.** 이 PC는 완전히 다른 프로젝트 PC입니다. Tailscale 관련 작업(메쉬 접속, portproxy, systemd 상시 구동)도 함께 홀드되었습니다. 이 PC에는 Tailscale이 설치되어 있지 않습니다.

---

## 현재 미결 항목

- [x] 데모 사이트에 `noindex` 메타 적용 (`_includes/head.html` 오버라이드 + `_config.yml`의 `noindex: true`)
- [ ] `fin.ragtailor.com` DNS 레코드 사전 생성
- [ ] 1장 본문 확정 — 사업 목적 / 주요 사업 내용 / 기대 효과 (현재 *작성 예정* 상태)
- [ ] 2~5장 페이지 골격 생성
- [ ] 실제 발주처 확인 후 표기 여부 결정
- [ ] 생성 파일 git 커밋 (현재 전부 untracked)
